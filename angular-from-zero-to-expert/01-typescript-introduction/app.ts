(function () {
    function greet(name: string) {
        console.table('Hello ' + name); // Hola Logan
    }

    const wolverine = {
        name: 'Logan'
    };

    greet(wolverine.name);

})();

